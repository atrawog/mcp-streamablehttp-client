# MCP HTTP-to-stdio Proxy Configuration

# MCP Server URL (required - this is all you need!)
MCP_SERVER_URL=https://everything.example.com/mcp

# Everything else is discovered automatically:
# - OAuth endpoints are discovered via RFC 8414 metadata
# - Client credentials are obtained via dynamic registration
# - Tokens are managed automatically

# Optional Configuration (defaults shown)
CLIENT_NAME=mcp-http-stdio
CLIENT_VERSION=0.1.0
SESSION_TIMEOUT=300  # seconds
REQUEST_TIMEOUT=30   # seconds
LOG_LEVEL=INFO
CREDENTIAL_STORAGE_PATH=~/.mcp/credentials.json
VERIFY_SSL=true